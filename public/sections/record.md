# the record

What this session has produced so far. Counters start at page load. Nothing is stored.

- cumulative energy (joules, this session)
- firing rate histogram (samples of r(t), this session)
- mean power by stimulus phase (watts, this session)
- peaks: highest power, highest rate

There is no global counter. Nothing leaves the browser.
